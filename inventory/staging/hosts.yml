---
all:
  children:
    matrix_servers:
      hosts:
        matrix-staging.example.com:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
          matrix_domain: staging.example.com
          matrix_homeserver_name: matrix-staging.example.com
          ssl_email: admin@example.com
          server_ip: 192.168.1.101
          ssh_port: 2222
  vars:
    ansible_become: yes
    ansible_become_method: sudo
    # Local deployment - run this on the target server
    # External SSH access via zerokaine user on configured port